import java.util.*;
public class Input {
    public static void main(String[] args) {
        Scanner sc=new Scanner(System.in);

        /*
        int[] arr=new int[5];

        // Array of primitives.

        System.out.println("Give inputs");

        // To take the input.
        for(int i=0;i<arr.length;i++){
            arr[i]=sc.nextInt();
        }
        // For Printing the Output.
        System.out.println("Output array");
        // for(int i=0;i<arr.length;i++){
        //     System.out.print(arr[i]+" ");
        // }

        System.out.println(Arrays.toString(arr));  //toString() method converts the array into the string then prints the array.  [1,2,3,4,5].

         */


        // ------------------Array of objects----------------------------------  


        /*
        System.out.println("Give inputs ");
        String[] arr=new String[5];
        for(int i=0;i<arr.length;i++){
            arr[i]=sc.nextLine();
        }

        // Output for array.
        System.out.println("Output array ");
        // for (String string : arr) {
        //     System.out.print(string+" ");  //Prints the array.
        // }

        // Modify
        arr[1]="ansh";

        System.out.println(Arrays.toString(arr));

         */
    }
}
